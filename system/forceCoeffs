FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      forceCoeffs;
}

forceCoeffs1
{
    type            forceCoeffs;
    libs            ("libforces.so");
    writeControl    timeStep;

    patches         (ahmedBody);
    rho             rhoInf;
    rhoInf          1.225;
    liftDir         (0 0 1);
    dragDir         (1 0 0);
    CofR            (0.472 0 0.169);
    pitchAxis       (0 1 0);
    magUInf         40;
    lRef            1.044;
    Aref            0.1121;
}
